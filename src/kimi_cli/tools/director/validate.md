Validate the director's output files for cross-file consistency.

Checks entities.json, events.json, states.json, and shots.json in the given project directory. Returns a validation report with errors and warnings.

**Parameters:**
- `project_path`: Absolute path to the project directory containing the director's output files.
