Check the status of a music generation job and optionally download completed songs (mp3).

Suno generates 2 songs per request. Use `download_dir` to download all completed songs.

**States:** `pending` → `processing` → `completed` / `failed`
