Generate an image using a configured image generation provider (e.g. Google Gemini).

Supports text-to-image generation with optional style hints, aspect ratio control, and reference images for guided generation.

Use this tool during the character design phase to create visual references that can later be used as input for image_to_video generation.
