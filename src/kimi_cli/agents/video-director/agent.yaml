version: 1
agent:
  extend: default
  name: video-director
  system_prompt_path: ./system.md
  system_prompt_args:
    ROLE_ADDITIONAL: ""
  tools:
    - "kimi_cli.tools.multiagent:Task"
    - "kimi_cli.tools.todo:SetTodoList"
    - "kimi_cli.tools.file:ReadFile"
    - "kimi_cli.tools.file:ReadMediaFile"
    - "kimi_cli.tools.file:WriteFile"
    - "kimi_cli.tools.file:Glob"
    - "kimi_cli.tools.file:Grep"
  subagents:
    screenwriter:
      path: ../screenwriter/agent.yaml
      description: "编剧 agent。Builds, edits, and validates Story Graph (story-graph.json) — the structured graph representation of a story. Converts user descriptions into graph with characters, events, states, camera directives, and audio. Can incrementally edit existing graphs and check for logical/consistency issues."
    video-creator:
      path: ../video-creator/agent.yaml
      description: "Generates videos from script to final output. Has access to GenerateVideo, CheckVideoJob, VideoEdit, GenerateImage for visuals, and GenerateMusic, CheckMusicJob, GenerateSpeech for audio (background music via Suno, TTS narration via Minimax)."
